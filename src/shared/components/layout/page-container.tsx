import {cn}from'@/lib/utils';export function PageContainer({className,children}:{className?:string;children:React.ReactNode}){return <div className={cn('mx-auto w-full max-w-[90rem] space-y-8 md:space-y-6',className)}>{children}</div>}export function Section({title,description,actions,children}:{title?:string;description?:string;actions?:React.ReactNode;children:React.ReactNode}){return <section className="space-y-5 md:space-y-4">{(title||description||actions)&&<div className="flex flex-col justify-between gap-4 md:flex-row md:items-end md:gap-3"><div>{title&&<h2 className="text-[1.375rem] font-semibold tracking-[-.025em] md:text-lg">{title}</h2>}{description&&<p className="mt-1.5 text-base leading-6 text-muted-foreground md:mt-1 md:text-sm">{description}</p>}</div>{actions}</div>}{children}</section>}
