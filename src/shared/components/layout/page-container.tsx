import {cn}from'@/lib/utils';export function PageContainer({className,children}:{className?:string;children:React.ReactNode}){return <div className={cn('mx-auto w-full max-w-[90rem] space-y-7 md:space-y-6',className)}>{children}</div>}export function Section({title,description,actions,children}:{title?:string;description?:string;actions?:React.ReactNode;children:React.ReactNode}){return <section className="space-y-4">{(title||description||actions)&&<div className="flex flex-col justify-between gap-3 md:flex-row md:items-end"><div>{title&&<h2 className="text-xl font-semibold tracking-tight md:text-lg">{title}</h2>}{description&&<p className="mt-1 text-base text-muted-foreground md:text-sm">{description}</p>}</div>{actions}</div>}{children}</section>}
