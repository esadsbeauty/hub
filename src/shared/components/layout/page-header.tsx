import type {ReactNode} from 'react';export function PageHeader({title,eyebrow,description,actions}:{title:string;eyebrow?:string;description?:string;actions?:ReactNode}){return <div className="flex flex-col justify-between gap-4 md:flex-row md:items-center"><div>{eyebrow&&<p className="mb-1 text-xs font-semibold md:text-[11px] uppercase tracking-[.18em] text-muted-foreground">{eyebrow}</p>}<h1 className="text-[1.75rem] font-semibold tracking-[-.035em] md:text-[1.75rem]">{title}</h1>{description&&<p className="mt-1 max-w-2xl text-base leading-relaxed text-muted-foreground md:text-sm">{description}</p>}</div>{actions&&<div className="grid grid-cols-1 gap-2 min-[390px]:grid-cols-2 md:flex md:flex-wrap md:items-center">{actions}</div>}</div>}
