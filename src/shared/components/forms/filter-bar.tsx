import { cn } from "@/lib/utils";export function FilterBar({children,className}:{children:React.ReactNode;className?:string}){return <div className={cn("flex flex-col gap-3 rounded-2xl bg-card p-3 shadow-soft md:flex-row md:items-center",className)}>{children}</div>}
