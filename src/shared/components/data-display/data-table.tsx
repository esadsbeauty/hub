import{cn}from'@/lib/utils';export type Column<T>={key:string;header:string;render:(row:T)=>React.ReactNode;className?:string};export function DataTable<T>({columns,data,onRowClick}:{columns:Column<T>[];data:T[];onRowClick?:(row:T)=>void}){return <div className="overflow-x-auto rounded-2xl bg-card shadow-soft"><table className="w-full min-w-[42rem] text-sm"><thead className="border-b border-border/60 text-left text-[11px] uppercase tracking-[.08em] text-muted-foreground"><tr>{columns.map((column)=><th key={column.key} className={cn('px-4 py-3 font-medium',column.className)}>{column.header}</th>)}</tr></thead><tbody>{data.map((row,index)=><tr key={index} onClick={()=>onRowClick?.(row)} className={cn('border-b border-border/45 last:border-0 hover:bg-muted/40',onRowClick&&'cursor-pointer')}>{columns.map((column)=><td key={column.key} className={cn('px-4 py-3.5',column.className)}>{column.render(row)}</td>)}</tr>)}</tbody></table></div>}
