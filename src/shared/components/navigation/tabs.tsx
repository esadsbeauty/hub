import {cn} from '@/lib/utils';export function Tabs<T extends string>({tabs,value,onChange}:{tabs:{value:T;label:string}[];value:T;onChange:(value:T)=>void}){return <div className="flex gap-5 overflow-x-auto border-b border-border/70">{tabs.map((tab)=><button key={tab.value} onClick={()=>onChange(tab.value)} className={cn('relative whitespace-nowrap pb-3 pt-1 text-sm font-medium text-muted-foreground smooth premium-focus',value===tab.value&&'text-foreground after:absolute after:inset-x-0 after:-bottom-px after:h-0.5 after:rounded-full after:bg-champagne')}>{tab.label}</button>)}</div>}
