import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { crmRepository } from './repository';
export function useCrmData() { return useQuery({ queryKey: ['crm'], queryFn: crmRepository.list }); }
export function useCrmActions() { const qc = useQueryClient(); const done = () => qc.invalidateQueries({ queryKey: ['crm'] }); return { createCompany: useMutation({ mutationFn: crmRepository.createCompany, onSuccess: done }), updateCompany: useMutation({ mutationFn: ({ id, data }: any) => crmRepository.updateCompany(id, data), onSuccess: done }), moveCompany: useMutation({ mutationFn: ({ id, status }: any) => crmRepository.moveCompany(id, status), onSuccess: done }), deleteCompany: useMutation({ mutationFn: crmRepository.deleteCompany, onSuccess: done }), duplicateCompany: useMutation({ mutationFn: crmRepository.duplicateCompany, onSuccess: done }), createContact: useMutation({ mutationFn: ({ companyId, data }: any) => crmRepository.createContact(companyId, data), onSuccess: done }), updateContact: useMutation({ mutationFn: ({ id, data }: any) => crmRepository.updateContact(id, data), onSuccess: done }), deleteContact: useMutation({ mutationFn: crmRepository.deleteContact, onSuccess: done }), createFollowUp: useMutation({ mutationFn: ({ companyId, data }: any) => crmRepository.createFollowUp(companyId, data), onSuccess: done }), updateFollowUp: useMutation({ mutationFn: ({ id, data }: any) => crmRepository.updateFollowUp(id, data), onSuccess: done }), createActivity: useMutation({ mutationFn: ({ companyId, data }: any) => crmRepository.createActivity(companyId, data), onSuccess: done }), addNote: useMutation({ mutationFn: ({ companyId, text }: any) => crmRepository.addNote(companyId, text), onSuccess: done }), addFile: useMutation({ mutationFn: ({ companyId, file }: any) => crmRepository.addFile(companyId, file), onSuccess: done }) }; }
