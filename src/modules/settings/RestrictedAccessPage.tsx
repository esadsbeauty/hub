import { LockKeyhole } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { PageContainer } from "@/shared/components/layout/page-container";
import { useAppState } from "@/shared/state/app-state-context";
import { BrandLogo } from "@/shared/components/brand/brand-logo";
export function RestrictedAccessPage() { const { status } = useAppState(); const suspended = status === "suspended"; const pending = status === "pending" || status === "unlinked" || status === "invited" || status === "inactive"; return <PageContainer><div className="grid min-h-[60dvh] place-items-center"><Card className="max-w-lg"><CardContent className="p-8 text-center sm:p-10"><BrandLogo className="mx-auto mb-7 w-36"/><LockKeyhole className="mx-auto mb-5 text-champagne-dark" size={36}/><p className="text-xs font-bold uppercase tracking-[.24em] text-champagne-dark">Governança ESADS</p><h1 className="mt-2 text-2xl font-bold">{suspended ? "Acesso suspenso" : pending ? "Acesso pendente" : "Acesso restrito"}</h1><p className="mt-3 text-muted-foreground">{suspended ? "Entre em contato com o administrador da organização." : pending ? "Seu cadastro ainda está aguardando aprovação do administrador da ESADS Beauty." : "Você não possui permissão para acessar esta área."}</p></CardContent></Card></div></PageContainer> }
