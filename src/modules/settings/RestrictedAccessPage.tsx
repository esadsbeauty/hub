import { LockKeyhole } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { PageContainer } from "@/shared/components/layout/page-container";
import { useAppState } from "@/shared/state/app-state";
export function RestrictedAccessPage() { const { status } = useAppState(); const suspended = status === "suspended"; const pending = status === "unlinked" || status === "invited" || status === "inactive"; return <PageContainer><div className="grid min-h-[60dvh] place-items-center"><Card className="max-w-lg"><CardContent className="p-8 text-center sm:p-10"><LockKeyhole className="mx-auto mb-5 text-champagne-dark" size={36}/><p className="text-xs font-bold uppercase tracking-[.24em] text-champagne-dark">Governança ESADS</p><h1 className="mt-2 text-2xl font-bold">{suspended ? "Acesso suspenso" : pending ? "Acesso pendente" : "Acesso restrito"}</h1><p className="mt-3 text-muted-foreground">{suspended ? "Entre em contato com o administrador da organização." : pending ? "Seu usuário ainda não está vinculado a uma organização ativa." : "Você não possui permissão para acessar esta área."}</p></CardContent></Card></div></PageContainer> }
