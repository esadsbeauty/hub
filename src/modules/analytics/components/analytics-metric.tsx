import type{LucideIcon}from'lucide-react';import{Card,CardContent}from'@/components/ui/card';export function AnalyticsMetric({label,value,detail,definition,comparison,icon:Icon,onClick}:{label:string;value:string;detail:string;definition:string;comparison?:string;icon?:LucideIcon;onClick?:()=>void}){const content=<CardContent className="p-5 text-left"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-medium text-muted-foreground" title={definition}>{label}</p><p className="mt-2 text-2xl font-semibold tracking-[-.035em]">{value}</p></div>{Icon&&<Icon className="mt-1 text-muted-foreground/45" size={17} strokeWidth={1.6}/>}</div><p className="mt-2 text-xs text-muted-foreground">{detail}</p>{comparison&&<p className="mt-1 text-xs font-medium text-success">{comparison}</p>}</CardContent>;return onClick?<button className="w-full rounded-2xl text-left premium-focus" onClick={onClick}><Card className="h-full transition-shadow duration-200 hover:shadow-[0_10px_32px_rgba(24,20,16,.08)]">{content}</Card></button>:<Card className="h-full">{content}</Card>}
