import{useEffect,useMemo,useState}from"react";import{MessageCircle,RefreshCw}from"lucide-react";import{Button}from"@/components/ui/button";import{useAppState}from"@/shared/state/app-state-context";import{ChatPanel}from"../components/chat-panel";import{ConversationList}from"../components/conversation-list";import{CrmPanel}from"../components/crm-panel";import{useSendWhatsAppMessage,useWhatsAppInbox,useWhatsAppMessages,useWhatsAppRealtime}from"../hooks/use-whatsapp-inbox";import type{WhatsAppConversation}from"../types";
export function WhatsAppInboxPage(){const{organizationId,role,isPlatformAdmin}=useAppState(),inbox=useWhatsAppInbox(),[selectedId,setSelectedId]=useState<string>(),[query,setQuery]=useState(""),[status,setStatus]=useState("all"),[details,setDetails]=useState(false);const selected=inbox.data?.conversations.find(item=>item.id===selectedId),messages=useWhatsAppMessages(selectedId),send=useSendWhatsAppMessage(selectedId),canReply=isPlatformAdmin||["owner","admin","manager","sales","operations","marketing"].includes(role);useWhatsAppRealtime();useEffect(()=>{setSelectedId(undefined);setDetails(false)},[organizationId]);const filtered=useMemo(()=>{const normalized=query.trim().toLocaleLowerCase("pt-BR");return(inbox.data?.conversations??[]).filter(item=>(status==="all"||item.status===status)&&(!normalized||`${item.contactName??""} ${item.waId}`.toLocaleLowerCase("pt-BR").includes(normalized)))},[inbox.data?.conversations,query,status]);const select=(conversation:WhatsAppConversation)=>{setSelectedId(conversation.id);setDetails(false)};if(inbox.isLoading)return<div className="grid min-h-[60dvh] place-items-center text-sm text-muted-foreground">Carregando Inbox WhatsApp…</div>;if(inbox.isError)return<div className="grid min-h-[60dvh] place-items-center text-center"><div><p className="font-semibold">Não foi possível carregar a Inbox.</p><p className="mt-2 text-sm text-muted-foreground">Verifique sua conexão e tente novamente.</p><Button className="mt-5" variant="outline" onClick={()=>void inbox.refetch()}><RefreshCw size={17}/>Tentar novamente</Button></div></div>;if(!inbox.data?.connection)return<div className="grid min-h-[60dvh] place-items-center text-center"><div className="max-w-md"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-champagne-soft text-champagne-dark"><MessageCircle/></span><h1 className="mt-5 text-2xl font-semibold">WhatsApp ainda não conectado</h1><p className="mt-3 leading-7 text-muted-foreground">Conecte uma conta do WhatsApp Business para receber conversas diretamente no CRM.</p><Button className="mt-6" disabled>Configurar WhatsApp</Button><p className="mt-2 text-xs text-muted-foreground">Configuração disponível em uma próxima etapa.</p></div></div>;return<div className="-mx-4 -my-6 min-[430px]:-mx-5 md:-mx-6 md:-my-8 lg:-mx-8"><div className="grid h-[calc(100dvh-4rem)] min-h-[34rem] overflow-hidden border-y bg-card md:grid-cols-[20rem_minmax(0,1fr)] xl:grid-cols-[20rem_minmax(0,1fr)_19rem]"><div className={selected?"hidden md:flex":"flex min-h-0"}><ConversationList items={filtered} selectedId={selectedId} query={query} status={status} onQuery={setQuery} onStatus={setStatus} onSelect={select}/></div><div className={!selected?"hidden md:block":"min-h-0"}><ChatPanel conversation={selected} messages={messages.data??[]} loading={messages.isLoading} sending={send.isPending} canReply={canReply} onSend={text=>send.mutateAsync(text)} onBack={()=>setSelectedId(undefined)} onDetails={()=>setDetails(true)}/></div><CrmPanel conversation={selected}/>{details&&<><button className="fixed inset-0 z-40 bg-black/40 xl:hidden" aria-label="Fechar informações" onClick={()=>setDetails(false)}/><CrmPanel conversation={selected} drawer onClose={()=>setDetails(false)}/></>}</div></div>}
