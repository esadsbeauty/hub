import{Search}from"lucide-react";import{Input}from"@/components/ui/input";import type{WhatsAppConversation}from"../types";
const preview=(conversation:WhatsAppConversation)=>conversation.lastMessage?.textBody||conversation.lastMessage&&`Mensagem de ${conversation.lastMessage.messageType}`||"Sem mensagens";const when=(value?:string)=>value?new Intl.DateTimeFormat("pt-BR",Date.now()-new Date(value).getTime()<86_400_000?{hour:"2-digit",minute:"2-digit"}:{day:"2-digit",month:"2-digit"}).format(new Date(value)):"";
export function ConversationList({items,selectedId,query,status,onQuery,onStatus,onSelect}:{items:WhatsAppConversation[];selectedId?:string;query:string;status:string;onQuery:(value:string)=>void;onStatus:(value:string)=>void;onSelect:(conversation:WhatsAppConversation)=>void}){return <section className="flex min-h-0 flex-1 flex-col border-r bg-card" aria-label="Conversas do WhatsApp"><header className="border-b p-4"><h1 className="text-xl font-semibold tracking-[-.03em]">WhatsApp</h1><label className="relative mt-4 block"><span className="sr-only">Buscar conversas</span><Search className="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" size={18}/><Input value={query} onChange={event=>onQuery(event.target.value)} className="pl-10" placeholder="Buscar nome ou telefone"/></label><div className="mt-3 flex gap-2" role="group" aria-label="Filtrar conversas">{[["all","Todas"],["open","Abertas"],["closed","Fechadas"]].map(([value,label])=><button key={value} type="button" onClick={()=>onStatus(value)} className={`premium-focus rounded-full px-3 py-1.5 text-xs font-semibold ${status===value?"bg-primary text-white":"bg-muted text-muted-foreground"}`}>{label}</button>)}</div></header><div className="min-h-0 flex-1 overflow-y-auto">{items.length===0?<div className="px-6 py-14 text-center"><p className="font-semibold">Nenhuma conversa recebida ainda.</p><p className="mt-2 text-sm leading-6 text-muted-foreground">As mensagens recebidas pelo WhatsApp aparecerão aqui.</p></div>:items.map(conversation=><button type="button" key={conversation.id} onClick={()=>onSelect(conversation)} className={`premium-focus w-full border-b px-4 py-4 text-left transition-colors ${selectedId===conversation.id?"bg-champagne-soft":"hover:bg-muted/60"}`}><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="truncate font-semibold">{conversation.contactName||conversation.waId}</p><p className="mt-0.5 truncate text-xs text-muted-foreground">{conversation.waId}</p></div><time className="shrink-0 text-[11px] text-muted-foreground">{when(conversation.lastMessageAt||conversation.lastMessage?.messageTimestamp||conversation.lastMessage?.createdAt)}</time></div><div className="mt-2 flex items-center justify-between gap-3"><p className="truncate text-sm text-muted-foreground">{preview(conversation)}</p><span className={`h-2 w-2 shrink-0 rounded-full ${conversation.status==="open"?"bg-emerald-500":"bg-muted-foreground/40"}`} aria-label={conversation.status==="open"?"Aberta":"Fechada"}/></div></button>)}</div></section>}
