import { useEffect } from "react";
import { siteUrl } from "@/config/site-url";
const setMeta=(key:string,value:string,property=false)=>{const selector=`meta[${property?"property":"name"}="${key}"]`;let node=document.head.querySelector<HTMLMetaElement>(selector);if(!node){node=document.createElement("meta");node.setAttribute(property?"property":"name",key);document.head.appendChild(node)}node.content=value};
const removeMeta=(key:string,property=false)=>document.head.querySelector(`meta[${property?"property":"name"}="${key}"]`)?.remove();
export function SeoHead({title,description,path,image,type="website",noIndex=false}:{title:string;description:string;path:string;image?:string;type?:"website"|"article";noIndex?:boolean}){useEffect(()=>{const canonical=`${siteUrl}${path}`;document.title=title;let link=document.head.querySelector<HTMLLinkElement>('link[rel="canonical"]');if(!link){link=document.createElement("link");link.rel="canonical";document.head.appendChild(link)}link.href=canonical;setMeta("description",description);setMeta("robots",noIndex?"noindex,nofollow":"index,follow");setMeta("og:title",title,true);setMeta("og:description",description,true);setMeta("og:type",type,true);setMeta("og:url",canonical,true);if(image)setMeta("og:image",image,true);else removeMeta("og:image",true);setMeta("twitter:card",image?"summary_large_image":"summary");setMeta("twitter:title",title);setMeta("twitter:description",description)},[title,description,path,image,type,noIndex]);return null}
