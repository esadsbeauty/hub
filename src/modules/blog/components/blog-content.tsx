import type { ReactNode } from "react";
const inline=(text:string):ReactNode[]=>{const parts=text.split(/(\*\*[^*]+\*\*|\*[^*]+\*|\[[^\]]+\]\(https?:\/\/[^)]+\))/g);return parts.map((part,index)=>{if(part.startsWith("**")&&part.endsWith("**"))return <strong key={index}>{part.slice(2,-2)}</strong>;if(part.startsWith("*")&&part.endsWith("*"))return <em key={index}>{part.slice(1,-1)}</em>;const link=part.match(/^\[([^\]]+)\]\((https?:\/\/[^)]+)\)$/);return link?<a key={index} href={link[2]} target="_blank" rel="noreferrer">{link[1]}</a>:part})};
export function BlogContent({content}:{content:string}){const lines=content.split("\n");const nodes:ReactNode[]=[];let list:string[]=[];const flush=()=>{if(list.length){nodes.push(<ul key={`list-${nodes.length}`}>{list.map((item,index)=><li key={index}>{inline(item)}</li>)}</ul>);list=[]}};lines.forEach((raw,index)=>{const line=raw.trim();if(line.startsWith("- ")){list.push(line.slice(2));return}flush();if(!line)return;if(line.startsWith("### "))nodes.push(<h3 key={index}>{inline(line.slice(4))}</h3>);else if(line.startsWith("## "))nodes.push(<h2 key={index}>{inline(line.slice(3))}</h2>);else if(line.startsWith("> "))nodes.push(<blockquote key={index}>{inline(line.slice(2))}</blockquote>);else nodes.push(<p key={index}>{inline(line)}</p>)});flush();return <div className="blog-prose">{nodes}</div>}
