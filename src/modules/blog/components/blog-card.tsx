import { ArrowUpRight } from "lucide-react";
import { Link } from "react-router-dom";
import { blogDataSource } from "../data-source";
import { readingMinutes, type BlogPost } from "../types";
export function BlogCard({post}:{post:BlogPost}){const image=blogDataSource.coverUrl(post.coverImagePath);return <article className="group overflow-hidden rounded-[1.5rem] bg-white shadow-soft"><Link to={`/blog/${post.slug}`} className="block premium-focus"><div className="aspect-[16/10] overflow-hidden bg-gradient-to-br from-[#171512] via-[#383026] to-[#c7a86b]">{image&&<img src={image} alt="" width="720" height="450" loading="lazy" className="h-full w-full object-cover transition duration-500 group-hover:scale-[1.025]"/>}</div><div className="p-6"><div className="flex flex-wrap items-center gap-2 text-xs font-semibold uppercase tracking-[.12em] text-champagne-dark"><span>{post.categoryName??"Estratégia"}</span><span className="text-border">•</span><time>{post.publishedAt?new Intl.DateTimeFormat("pt-BR",{day:"2-digit",month:"short",year:"numeric"}).format(new Date(post.publishedAt)):""}</time><span className="text-border">•</span><span>{readingMinutes(post.content)} min</span></div><h2 className="mt-4 text-[1.45rem] font-semibold leading-tight tracking-[-.035em] md:text-2xl">{post.title}</h2><p className="mt-3 line-clamp-3 text-base leading-7 text-muted-foreground">{post.excerpt}</p><span className="mt-6 inline-flex items-center gap-2 text-sm font-semibold">Ler artigo <ArrowUpRight size={17}/></span></div></Link></article>}
