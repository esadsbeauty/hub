import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Select}from"@/components/ui/select";import type{ProductLeadFilters as Filters}from"./types";import{statusLabels}from"./utils";
export function ProductLeadFilters({value,sources,campaigns,onChange}:{value:Filters;sources:string[];campaigns:string[];onChange:(next:Filters)=>void}){const set=(patch:Partial<Filters>)=>onChange({...value,...patch,page:1});return <section aria-label="Filtros de leads" className="grid gap-2 rounded-2xl border bg-card p-3 sm:grid-cols-2 xl:grid-cols-6"><Input aria-label="Buscar leads" placeholder="Nome, WhatsApp, e-mail ou negócio" value={value.query} onChange={e=>set({query:e.target.value})}/><Select aria-label="Filtrar por status" value={value.status} onChange={e=>set({status:e.target.value as Filters["status"]})}><option value="">Todos os status</option>{Object.entries(statusLabels).map(([key,label])=><option key={key} value={key}>{label}</option>)}</Select><Select aria-label="Filtrar por origem" value={value.source} onChange={e=>set({source:e.target.value})}><option value="">Todas as origens</option>{sources.map(item=><option key={item}>{item}</option>)}</Select><Select aria-label="Filtrar por campanha" value={value.campaign} onChange={e=>set({campaign:e.target.value})}><option value="">Todas as campanhas</option>{campaigns.map(item=><option key={item}>{item}</option>)}</Select><Select aria-label="Filtrar por período" value={value.period} onChange={e=>set({period:e.target.value as Filters["period"]})}><option value="">Todo o período</option><option value="7">Últimos 7 dias</option><option value="30">Últimos 30 dias</option></Select><div className="flex gap-2"><Select aria-label="Ordenar leads" value={value.sort} onChange={e=>set({sort:e.target.value as Filters["sort"]})}><option value="newest">Mais recentes</option><option value="oldest">Mais antigos</option><option value="updated">Última atualização</option></Select><Button variant="ghost" onClick={()=>onChange({query:"",status:"",source:"",campaign:"",period:"",sort:"newest",page:1,pageSize:value.pageSize})}>Limpar</Button></div></section>}
