import{Button}from"@/components/ui/button";import{Input}from"@/components/ui/input";import{Select}from"@/components/ui/select";import type{OrganizationFilters as Filters}from"./types";
export function OrganizationFilters({value,plans,onChange}:{value:Filters;plans:{id:string;name:string}[];onChange:(x:Filters)=>void}){const set=(patch:Partial<Filters>)=>onChange({...value,...patch,page:1});return <section className="grid gap-2 rounded-2xl border bg-card p-3 sm:grid-cols-2 xl:grid-cols-4"><Input aria-label="Buscar organizações" placeholder="Nome, slug ou e-mail do owner" value={value.query} onChange={e=>set({query:e.target.value})}/><Select aria-label="Filtrar por tipo" value={value.type} onChange={e=>set({type:e.target.value})}><option value="">Todos os tipos</option><option value="production">Produção</option><option value="test">Teste</option><option value="demo">Demo</option></Select><Select aria-label="Filtrar por status operacional" value={value.status} onChange={e=>set({status:e.target.value})}><option value="">Todos os acessos</option><option value="active">Ativas</option><option value="suspended">Suspensas</option></Select><Select aria-label="Filtrar por plano" value={value.planId} onChange={e=>set({planId:e.target.value})}><option value="">Todos os planos</option>{plans.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</Select><Select aria-label="Filtrar por assinatura" value={value.subscriptionStatus} onChange={e=>set({subscriptionStatus:e.target.value})}><option value="">Todas as assinaturas</option>{["active","pending","past_due","suspended","cancelled"].map(x=><option key={x}>{x}</option>)}</Select><Select aria-label="Filtrar por criação" value={value.created} onChange={e=>set({created:e.target.value})}><option value="">Qualquer data</option><option value="30">Últimos 30 dias</option><option value="90">Últimos 90 dias</option></Select><Select aria-label="Ordenar organizações" value={value.sort} onChange={e=>set({sort:e.target.value as Filters["sort"]})}><option value="newest">Mais recentes</option><option value="oldest">Mais antigas</option><option value="name">Nome</option><option value="updated">Última atualização</option></Select><Button variant="ghost" onClick={()=>onChange({...value,query:"",type:"",status:"",planId:"",subscriptionStatus:"",created:"",sort:"newest",page:1})}>Limpar filtros</Button></section>}
