function zonedParts(date:Date,timeZone:string){const p=Object.fromEntries(new Intl.DateTimeFormat("en-CA",{timeZone,year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",hourCycle:"h23"}).formatToParts(date).map(x=>[x.type,x.value]));return{year:+p.year,month:+p.month,day:+p.day,hour:+p.hour,minute:+p.minute}}
export function toZonedInput(iso:string,timeZone:string){const p=zonedParts(new Date(iso),timeZone);return`${p.year}-${String(p.month).padStart(2,"0")}-${String(p.day).padStart(2,"0")}T${String(p.hour).padStart(2,"0")}:${String(p.minute).padStart(2,"0")}`}
export function zonedInputToIso(value:string,timeZone:string){const[y,m,d,h,min]=value.split(/[-T:]/).map(Number),wanted=Date.UTC(y,m-1,d,h,min);let instant=new Date(wanted);for(let i=0;i<2;i++){const p=zonedParts(instant,timeZone),shown=Date.UTC(p.year,p.month-1,p.day,p.hour,p.minute);instant=new Date(instant.getTime()+wanted-shown)}return instant.toISOString()}
