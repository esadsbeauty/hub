import * as React from 'react';import {cn} from '@/lib/utils';export const Input=React.forwardRef<HTMLInputElement,React.InputHTMLAttributes<HTMLInputElement>>(({className,...p},ref)=><input ref={ref} className={cn('h-11 w-full rounded-xl border bg-white/70 px-3 text-sm premium-focus placeholder:text-muted-foreground dark:bg-white/5',className)}{...p}/>);Input.displayName='Input';
