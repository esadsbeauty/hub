import * as React from 'react';import {cn} from '@/lib/utils';export const Select=React.forwardRef<HTMLSelectElement,React.SelectHTMLAttributes<HTMLSelectElement>>(({className,children,...props},ref)=><select ref={ref} className={cn('h-14 w-full rounded-xl border border-border/90 bg-card px-4 text-base md:h-11 md:px-3 md:text-sm premium-focus disabled:bg-muted/60',className)}{...props}>{children}</select>);Select.displayName='Select';
