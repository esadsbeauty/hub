import * as React from 'react';import {cn} from '@/lib/utils';export const Select=React.forwardRef<HTMLSelectElement,React.SelectHTMLAttributes<HTMLSelectElement>>(({className,children,...p},ref)=><select ref={ref} className={cn('h-11 w-full rounded-xl border bg-white/70 px-3 text-sm premium-focus dark:bg-white/5',className)}{...p}>{children}</select>);Select.displayName='Select';
